  void main () {
  // Demonstrate various statements
  // (not supposed to do anything useful!)
    int age;
    bool candrive;
    read("How old are you, and can you drive? ", age, candrive);
    if (age == 18) {
      write("can vote");
      if ( ! candrive) write(" but can't drive");
    }
    else
      if (age == 21) {
        write("party time!");
        int headache = 0, strain = 0;
        for (int beers = 20; beers > 0; beers--) {
          strain++; headache++;
          if (strain % 8 == 0) {
            write("That\'s better"); strain = 0;
          }
        }
      }
    else if ((age > 21) && (age < 40)) write("not over the hill");
    elsif (age > 70)
      write("alas over the hill");
    else
      write("boring");
  } // main
